# شرح رفع النظام على GitHub والحصول على ملف EXE

النظام جاهز بالكامل في هذا المجلد: الكود + الواجهة + **ملفان تلقائيان يبنيان الـ EXE وينشران عرضاً مباشراً على GitHub**.
كل ما تحتاجه: حساب GitHub مجاني + 5 دقائق. لا تحتاج تثبيت أي شيء على جهازك للحصول على الـ EXE.

---

## الخطوة 1 — إنشاء مستودع جديد على GitHub

1. افتح <https://github.com/new>
2. Repository name: `attendance-dump` (أو أي اسم)
3. اختر **Private** أو **Public** حسب رغبتك
4. **مهم:** أزل علامة ✔ عن "Add a README" (لأن المجلد يحتوي README جاهزاً)
5. اضغط **Create repository**

## الخطوة 2 — رفع الكود (نسخ ولصق ثلاثة أوامر)

افتح موجه الأوامر (cmd) داخل مجلد `attendance_system` بعد فك ضغط الملف، ثم نفّذ
(استبدل `اسمك` باسم مستخدمك في GitHub):

```bat
cd attendance_system
git remote add origin https://github.com/اسمك/attendance-dump.git
git push -u origin main
```

> سأنذرك GitHub لطلب تسجيل دخول بالمتصفح — اضغط "Sign in with your browser" ووافق.

## الخطوة 3 — الحصول على ملف EXE جاهز للتنزيل (تلقائي بالكامل)

بمجرد انتهاء الرفع، سيعمل ملف `.github/workflows/build-exe.yml` تلقائياً:

1. افتح صفحة المستودع ← تبويب **Actions**
2. اضغط على أول عملية اسمها **"بناء EXE لويندوز"** (تستغرق 3-5 دقائق)
3. عند اكتمالها بنجاح ✔ انزل إلى قسم **Artifacts** في أسفل الصفحة
4. نزّل **AttendanceDump-Windows-EXE** ← هذا هو ملف الـ `AttendanceDump.exe` حقيقي مبني على ويندوز!

### لجعل الـ EXE يظهر في صفحة Releases (أفضل للمشاركة):

```bat
git tag v2.0
git push origin v2.0
```

سيُبنى EXE جديد ويُرفق تلقائياً في صفحة **Releases** مع رابط تنزيل دائم يمكنك إرساله لأي شخص.

## الخطوة 4 — عرض تجريبي مباشر على الإنترنت (اختياري ومجاني)

ملف `pages-demo.yml` ينشر واجهة النظام (وضع عرض ببيانات تجريبية) على:

```
https://اسمك.github.io/attendance-dump/
```

التفعيل: إعدادات المستودع **Settings ← Pages ← Source: GitHub Actions** ثم أعد تشغيل
عملية "عرض تجريبي" من تبويب Actions. (إنه عرض توضيحي للواجهة فقط — التوليد الفعلي يتم في البرنامج).

---

## التشغيل على أي جهاز بعد تنزيل الـ EXE

1. نزّل `AttendanceDump.exe` (من Actions أو Releases)
2. ضعه في مجلد وانسخ إليه `settings.json` إن كان عندك (أو دعه ينشئه)
3. شغّل — يحتاج ويندوز 10/11 (مكوّن Edge WebView2 مثبت مسبقاً في الأغلب)
4. إذا لم تفتح نافذة سطح المكتب لأي سبب: افتح cmd ونفّذ `AttendanceDump.exe --browser`

> **ملاحظة أمان ويندوز:** أول تشغيل قد يظهر تحذير SmartScreen — اضغط "More info" ثم "Run anyway"
> لأن الملف مبني بنفسك وغير موقّع بشهادة تجارية.

---

## تحديث الكود لاحقاً

أي تعديل تجريه ثم تنفّذ `git add . && git commit -m "وصف" && git push`
سيعيد بناء الـ EXE تلقائياً بنسخة جديدة.

## بدائل بدون GitHub

- بناء EXE محلياً على أي جهاز فيه Python: انقر مرتين على **build.bat**
- تشغيل مباشر بدون بناء: انقر مرتين على **run.bat**
- تجربة الواجهة فوراً بدون أي تثبيت: افتح ملف **تشغيل_تجريبي_بدون_تثبيت.html** في المتصفح
